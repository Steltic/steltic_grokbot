# NEVER commit copyrighted specification content (see NOTICE.md)
standards/
converted/
documents/standards/
**/documents/standards/
search/spec_fts.sqlite
**/spec_fts.sqlite
queue/

# environments & caches
.venv/
venv/
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# pack_distributable.py writes a standalone bundle (its own scripts/ copy included) to --out.
# One was once committed here, and post_install copied it over the workspace -- reverting the
# retrieval code to that snapshot on every install. The bundle is built at packing time, from
# scripts/; it does not belong inside the repo.
engineering_rag_phase2/scripts/
